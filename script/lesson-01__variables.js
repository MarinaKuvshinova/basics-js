'use strict';

// variables
const PERIOD = 6,
      DEPOSIT = 8.5;

let money = 250,
    income = 1250,
    mission = 'work as a react developer',
    addExpenses = (expenses) => {
        console.log('your expenses: ' + expenses);
    };

//message in alert
alert('your mission: ' + mission);
//message in console
addExpenses(500);
